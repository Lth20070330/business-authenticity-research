# 两人协作约定

## 角色

- A：数据渠道、来源登记、字段治理、标注规范与案例库。
- B：特征与基线、仿真、实验实现和复现。
- 共同：研究问题、数据划分、结果解释、比赛与论文写作。

A/B 为临时职责，不绑定身份；在任务中写明实际负责人。涉及标签、数据划分和结果解读的改动必须互相复核，避免一个人同时定义规则、生成真值并证明规则有效。

## 工作流程

1. 从更新后的 `main` 创建分支，例如 `research/data-audit`、`feat/baseline`、`docs/paper-outline`。自动化助手默认使用 `codex/` 前缀。
2. 每个 Issue 写明目标、负责人、输入、输出和验收标准；对应待办清单编号。
3. 每次提交聚焦一个问题，打开 PR 说明改动与验证，请另一位成员审阅。
4. 合并前解决意见、检查数据泄漏与敏感文件；阶段完成后更新待办与决策记录。

初始框架直接建立于 main；后续建议采用上述 PR 流程。仓库尚未配置强制分支保护，不能将此文档视为已启用的技术限制。

## 文档和实验

- 优先中文文档，字段和模块名使用稳定英文命名。
- 区分「计划」「假设」「观察结果」「已验证结论」。不得填写虚构指标。
- 实验必须记录代码提交、数据版本、划分版本、配置、随机种子与失败实验。
- 修改研究边界、主指标或标签口径时，先更新决策记录，再运行新实验。
- 笔记本提交前清空可能包含真实记录的输出；原始数据、密钥、导出流水不进 Git。
- `.gitignore` 不是安全扫描器；每次提交前检查 `git diff --cached`。

## 加入协作者

仓库所有者可在 GitHub Settings → Collaborators 中邀请另一位成员。需由所有者确定具体账号；本框架不预设协作者身份或自动发送邀请。
